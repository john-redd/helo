import React, {useState} from 'react';
import styled from 'styled-components';

const Form = () => {
  return (
    <FormMC>
      Form
    </FormMC>
  )
}

export default Form;

const FormMC = styled.section`

`